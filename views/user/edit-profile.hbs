<section>
    <div class="profile-container mt-3">
        {{!-- <h2>User Profile</h2>
        <div class="profile-info">
            <div><strong>Name:</strong> {{user.name}}</div>
            <div><strong>Email:</strong> {{user.email}}</div>
            <div class="edit-form"> <a class="btn btn-success" type="submit">Edit Profile</a></div>
        </div> --}}
        <div class="edit-form">
            <h3>Edit Profile</h3>
            <form action="/profile/edit-profile" method="post">
                <label for="name">Name:</label>
                <input type="text" id="name" name="newName" value="{{user.name}}" required>

                <label for="email">Email:</label>
                <input type="email" id="email" name="newEmail" value="{{user.email}}" required>

                <input type="email" name="email" value="{{user.email}}" hidden>
                <button type="submit">Save Changes</button>
            </form>
        </div>
    </div>
</section>
<style>

    .profile-container {
        max-width: 600px;
        background-color: white;
        margin: 0 auto;
        padding: 20px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        border-radius: 8px;
    }

    .profile-info,
    .edit-form {
        margin: 20px 0;
    }

    .profile-info div {
        margin: 10px 0;
    }

    .edit-form label {
        display: block;
        margin: 10px 10px 5px;
    }

    .edit-form input[type="text"],
    .edit-form input[type="email"] {
        width: 100%;
        padding: 10px;
        margin: 5px 0 10px;
        border: 1px solid #ccc;
        border-radius: 4px;
    }

    .edit-form button {
        background-color: #0ed14f;
        color: #fff;
        padding: 10px 15px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
    }

    .edit-form button:hover {
        background-color: #407643;
    } 
</style>